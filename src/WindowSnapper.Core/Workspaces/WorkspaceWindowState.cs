namespace WindowSnapper.Core.Workspaces;

/// <summary>
/// Describes the persisted window state for a workspace snapshot.
/// </summary>
public enum WorkspaceWindowState
{
    Normal = 0,
    Maximized = 1,
    Minimized = 2
}
