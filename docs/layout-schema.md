# Layout Schema

The layout schema will be introduced with the layout engine implementation.

The first supported layout format will use normalized zone coordinates from `0.0` to `1.0` and calculate target rectangles from a monitor work area.
